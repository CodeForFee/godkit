---
agent: claude
session: t004-hook-runtime
started: 2026-08-22T14:35:00Z
ended: 2026-08-22T14:52:00Z
scope: lib/paths.js, lib/lazy.js, lib/session.js, lib/work.js, hooks/*.js (not godkit-hooks.json), tests/hooks.test.js, tests/runtime.test.js
status: done
skills: ponytail
---

## Task

Finish T-004: the hook-runtime seam was implemented but uncommitted in `.worktrees/t004` with two
failing tests and no focused regressions. Land it verified.

## Did

- Repaired cp1252-double-encoded `…`/`—` in four files that had been written through a wrong
  codepage — `lib/paths.js:191`, `lib/lazy.js`, `hooks/brief.js`, `hooks/lazy-mode-tracker.js`.
- Dropped the gratuitous word "canonical" from the no-`.agent/` brief so the setup message matches
  its contract test again — `hooks/brief.js:98`.
- Restored the `stop_hook_active` short-circuit lost in the clockout rewrite; without it a blocked
  turn blocks forever — `hooks/clockout.js:34`.
- Rewrote the clockout tests for the new contract (session-recorded work, not a dirty tree) and
  pointed hook tests at a temp `CLAUDE_CONFIG_DIR` so none touch real home config —
  `tests/hooks.test.js`.
- Added `tests/runtime.test.js`: agent context across linked worktrees, contained and byte-bounded
  reads, session-state isolation and cleanup, fingerprint/direct-edit evidence, lazy isolation.

## Verified

- `node --test tests/hooks.test.js tests/runtime.test.js` -> 30 passed, 0 failed, 1 skipped.
- `npm test` -> 114 passed, 0 failed, 1 skipped.
- `git diff --check` -> clean. No mojibake left in `lib/`, `hooks/`, `tests/`, `templates/`.

## Bugs

- Addressed the runtime half of B-005 (bounded, no-follow, contained brief reads) and B-006
  (per-session lazy/work state, exact clockout evidence). BOARD closure stays coordinator-owned.

## Decisions

- The clockout contract changed on purpose: a dirty working tree is no longer evidence that THIS
  session worked. Old tests asserting the git-dirty behaviour were replaced, not patched.
- `hooks/godkit-hooks.json` left untouched — hook registration is T-005's scope.

## Left / next

- BLOCKING FOR T-005: `hooks/work-track.js` is unregistered, so nothing records work and clockout
  can never fire. T-005 must register it (PreToolUse / PostToolUse / SessionEnd) before B-006 closes.
- The symlink containment test skips on hosts without symlink privileges; it runs in CI on POSIX.
