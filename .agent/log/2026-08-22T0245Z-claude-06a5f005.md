---
agent: claude-opus-5
session: 06a5f005
started: 2026-08-22T01:50Z
ended: 2026-08-22T02:45Z
scope: '*'
status: done
---

## Task

Refactor the subagent skill set into godkit: one npm-published harness giving Claude Code, Cursor, Codex and Antigravity shared memory, a project map, and a handoff protocol.

## Did

- package skeleton — `package.json` (godkit 1.0.0, `bin`, zero deps, `files` allowlist), `LICENSE`, `.gitattributes` pinning LF so rule copies compare byte-equal across platforms
- `AGENTS.md` as the single rule source; `scripts/sync-rules.js` generates the four host copies and `--check` fails CI on drift
- `lib/paths.js` — project root and `.agent/` resolution, with the linked-worktree redirect to the main repo root
- `lib/graph.js` — memory core: load, normalize, sanitize, save, render. Holds both destructive-failure guards
- `lib/freshness.js` — git-only staleness plus the SKIP/PARTIAL/ARCHITECTURE/FULL classifier
- `lib/scan.js` + `lib/batch.js` — deterministic walk, import resolution, locality batching
- `hooks/{brief,clockout,map-watch}.js` and `hooks/install.js` (idempotent settings merge, refuses unparseable JSON)
- `bin/godkit.js` — init, scan, save, install, doctor, uninstall
- 9 skills under `skills/`, 2 agent prompts under `agents/`, 9 `commands/*.toml`
- host manifests for Claude Code, Codex and Antigravity; `docs/agent-portability.md`
- 65 tests (`node:test`, zero deps), CI matrix on windows+linux x node 18/22/24, OIDC publish workflow
- removed the seven `subagent-*` skill dirs and `hooks/agent-brief.js`, superseded

## Verified

- `npm test` → 65 pass, 0 fail (includes `scripts/sync-rules.js --check` and `check-versions.js` via pretest)
- `npm pack --dry-run` → 44 files, 53.4 kB; extracted the tarball to a clean dir and ran the CLI and all three hooks from it alone
- worktree redirect: created a real linked worktree; `gitRoot()` returned the **main** root from inside it
- cross-tool handoff, end to end: a simulated Claude session claimed `src/a/*`, logged, and posted to THREAD; the brief hook then showed an arriving Cursor the open claim, B-001 already fixed with its root cause, B-002 still open, and the direct message
- enforcement: clockout blocks on changed-files-without-log, goes silent once the log exists, never blocks twice (`stop_hook_active`), and ignores changes confined to `.agent/`
- destructive-failure guards: corrupted `graph.json` → `godkit save` exits 1 and leaves the damaged bytes untouched; a PARTIAL save that mentions one file keeps the other file's nodes, layers and tour
- dogfooded: built godkit's own map (30 nodes, 12 edges, 6 layers) and answered "where is staleness decided" with two greps and no source file opened

## Bugs

- fixed B-001 — a path on a different drive leaked in full into the committed graph. Root cause was `sanitizePath` in lib/graph.js: on Windows `path.relative` across drives cannot express a relative path and returns the absolute target, which passed the bare `..` check. Found by its own test, not by review.

## Decisions

See the board. The load-bearing ones: `.agent/` keeps its name; zero runtime dependencies; rule files are generated rather than copied; signatures derive from the graph so there is no second store to desync; `meta.json` written last.

## Left / next

- **not published.** `npm publish` has not been run — `godkit` is confirmed free on npm, and the OIDC workflow fires on a `v*` tag. Publishing is the user's call.
- **not installed on this machine.** `godkit install` and `node hooks/install.js` were never run against the real `~/.claude`, `~/.agents` or `~/.gemini` — deliberately, to avoid changing the user's setup without asking. `godkit doctor` reports all four as not installed.
- the live cross-tool test used simulated sessions. A real Cursor or Codex session resuming this repo has not been run.
- `docs/`, `tests/`, `.github/` and the plugin manifests are intentionally outside the npm `files` allowlist: npm ships the CLI and skills, the git repo serves the plugin marketplace path.
- the map covers code, skills and named docs; `templates/` and `tests/` are represented by their layer rather than per-file nodes.
