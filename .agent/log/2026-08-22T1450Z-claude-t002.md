---
agent: "unrecorded"   # predates the model-id rule; not back-filled
session: t002-evolve-safety
started: 2026-08-22T14:36:00Z
ended: 2026-08-22T14:55:00Z
scope: lib/evolve.js, templates/log.md, tests/evolve.test.js
status: done
skills: godkit-lazy
---

## Task

Land T-002: the evolve-safety seam was complete but uncommitted in `.worktrees/t002`.

## Did

- Verified the seam rather than rewriting it: owned SHA-256 snapshot markers, stricter frontmatter
  parsing, widened executable/symlink scanning, quoted log-template scalars.
- Confirmed the diff stays inside the three claimed files and leaves no fixture directories.

## Verified

- `node --test tests/evolve.test.js` -> 36 passed, 0 failed.
- `npm test` -> 109 passed, 0 failed.
- `git diff --check` -> clean. No double-encoded characters in the touched files.

## Bugs

- Addresses B-002 (foreign projections preserved, snapshots owned) and B-010 (fixture cleanup).
  BOARD closure stays coordinator-owned.

## Decisions

- Left the hand-rolled frontmatter parser in place. Zero runtime dependencies is a binding
  constraint, so a YAML library is not an option; recorded it as a ceiling in the task handoff
  instead of widening it speculatively.

## Left / next

- T-007 must update README and `skills/godkit-evolve/SKILL.md`: `origin` and `enabled` are now
  required frontmatter, so a skill without frontmatter no longer links.
