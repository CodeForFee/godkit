---
agent: codex-kuhn
session: t003-graph-scan
started: 2026-08-22T14:18:00Z
ended: 2026-08-22T14:28:10Z
scope: lib/graph.js, lib/scan.js, tests/graph.test.js, tests/scan.test.js
status: done
skills:
---

## Task

Make graph persistence transactional and portable, and make the documented ignore subset safe.

## Did

- Rejected absolute graph IDs before normalization/write while retaining `filePath` sanitization — `lib/graph.js:101-140`.
- Made duplicate typed edges last-wins and exported a same-directory atomic writer used by `saveGraph` — `lib/graph.js:155-213`.
- Replaced unsafe ignore regex construction with the supported wildcard/root/path/directory subset; unsupported negation and `**` rules are skipped — `lib/scan.js:48-80`.
- Added focused portability, rollback, deduplication, wildcard, anchoring, path, and directory regressions — `tests/graph.test.js:64`, `tests/scan.test.js:81`.

## Verified

- `node --test tests/graph.test.js tests/scan.test.js` -> 25 passed, 0 failed.
- `npm test` -> rule/version checks clean; 100 passed, 0 failed.
- `git diff --check` -> clean.

## Bugs

- Addressed the T-003 atomic-write portion of B-003 and ignore-matcher portion of B-004; BOARD closure remains coordinator-owned.

## Decisions

- Export name is `atomicWriteFile(file, data)` so T-006 can reuse the same transaction boundary.

## Left / next

- T-006 can reuse `atomicWriteFile`; negation and `**` stay intentionally unsupported unless a future task adopts a full matcher.
