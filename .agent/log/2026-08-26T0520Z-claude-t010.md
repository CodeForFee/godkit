---
agent: "unrecorded"   # predates the model-id rule; not back-filled
session: "claude-t010"
started: "2026-08-26T0430Z"
ended: "2026-08-26T0520Z"
scope: "lib/hotspots.js, bin/godkit.js, lib/evolve.js, skills/godkit-refactor/*, skills/godkit-evolve/SKILL.md, skills/godkit-help/SKILL.md, commands/godkit-refactor.toml, tests/hotspots.test.js, tests/graph.test.js, README.md, CHANGELOG.md, package.json, .gitignore, .agent/graph.json, .agent/MAP.md"
status: "done"
skills: ""
---

## Task

Audit the repo for public release, then split source-code evolution out of `godkit-evolve` into
its own `godkit-refactor` seam, and fix everything blocking a 1.0.0 publish.

## Did

- Settled the working tree left over from removing the watch dashboard — reverted
  `hooks/brief.js` and deleted the untracked `lib/frontmatter.js`. Committing `brief.js` alone
  would have crashed `SessionStart` for every user installing from a clean checkout, because
  `npm pack` bundles untracked files and CI does not.
- Kept `scope` raw and added `did` to the log record — lib/evolve.js:745. `taskTokens` unchanged.
- New `lib/hotspots.js` (121 lines): ranks code files `blamed x2 + touched` from the log stream,
  with fan-in from the map. Map is the file universe, so path-shaped prose scores nothing and
  glob claims expand to real files — lib/hotspots.js:61, lib/hotspots.js:50.
- New `cmdRefactor` — bin/godkit.js:475, plus HELP at bin/godkit.js:600 and dispatch at
  bin/godkit.js:625.
- New `skills/godkit-refactor/SKILL.md` and `commands/godkit-refactor.toml`.
- `skills/godkit-evolve/SKILL.md` description and Boundaries now say it evolves procedures, and
  name godkit-refactor for code. That confusion is what started this task.
- New `tests/hotspots.test.js`, 6 tests.
- Fixed B-011: `.agent/MAP.md` was rendering `1. **** — ` eight times. Rewrote the tour as eight
  real entries and derived layer membership from the graph — 169 nodes placed, 0 unplaced.
- README: skill count 14 -> 15, refactor rows in the Skills and CLI tables, a "Refactor —
  evolving the code" section, plugin-marketplace install, `docs/agent-portability.md` linked, and
  the `agents/` prompts documented for the first time — README.md:110, README.md:170,
  README.md:271.
- `CHANGELOG.md` written and added to the `files` allowlist — package.json:44.
- `tests/graph.test.js:42` no longer carries the maintainer's real Windows username.
- `.worktrees/` added to `.gitignore`.

## Verified

- `npm test` -> `tests 167 · pass 166 · fail 0 · skipped 1`. The skip is symlink privileges at
  tests/runtime.test.js:84, unchanged from before this session.
- `node --test tests/hotspots.test.js` -> `pass 6 · fail 0`.
- `node bin/godkit.js refactor` -> ranked table, `bin/godkit.js` and `hooks/install.js` tied at 6.
- `node bin/godkit.js doctor` -> `skills in package: 15`.
- `npm pack --dry-run` -> `total files: 81`; `lib/hotspots.js` present, `lib/frontmatter.js`
  absent, `CHANGELOG.md` present.
- `sed -n '/## Start here/,/## Recall/p' .agent/MAP.md` -> eight real entries, no empty `****`.

## Bugs

- fixed B-011 — `.agent/MAP.md` renders `1. **** — ` eight times. Root cause is the tour and
  layer data in `.agent/graph.json`, which the architect pass emitted as empty shells; `renderMap`
  in `lib/graph.js` renders faithfully and was left alone.

## Decisions

- `godkit-evolve` keeps its meaning and `godkit-refactor` is a separate skill and command —
  the user's call between renaming, redefining, or splitting. Both read the same log stream; one
  evolves procedures, one evolves code.
- Docs are filtered out of the hotspot ranking. `README.md` scored 7 and outranked every source
  file, and there is no refactor at the end of that row. Asserted in a test.
- `renderMap` was not hardened against an empty tour. A guard there turns a bad architect pass
  from visible into silent; B-011 was a data defect and is fixed as data.
- Version stays `1.0.0`. Nothing has ever been published, so there is no released version to bump
  away from and the whole seam ships in the first release.

## Release

Correcting an audit finding from earlier in this same session: I reported `main` as 25 commits
behind and the public repo as showing a pre-hardening state. That was wrong — I read a stale
`origin/main` remote-tracking ref without fetching. `origin/main` had already been brought up to
date through PRs #2–#13, all of which are merge commits whose tree is byte-identical to
`dfafab0`. `git diff --stat dfafab0 origin/main` is empty. The public repo was never stale.

- Rebased the two commits of this session onto `origin/main` and pushed: `4066a42..78ae919`.
  Tree unchanged by the rebase (`git diff --stat fc26d31 HEAD` empty), 167 tests still green,
  map still current afterwards.
- GitHub topics set (ai-agents, claude-code, cursor, codex, developer-tools, agent-memory,
  multi-agent) and homepage pointed at the npm package page.
- Deleted six local and three remote `godkit/*` branches, all confirmed content-identical to
  main first. `main` is now the only branch on either side.

## Left / next

- **Not tagged, not published, and that is the only thing left.** Register `CodeForFee/godkit` +
  `publish.yml` as a trusted publisher on npmjs.com **before** pushing a `v1.0.0` tag — the
  publish workflow uses OIDC with no `NODE_AUTH_TOKEN` and will fail at the last step otherwise.
  The name was free (registry 404).
- This machine still has 3 of 10 hooks registered from an older install.
