---
agent: "claude-opus-5"
session: "dddfc45c"
started: "2026-08-31T0210Z"
ended: "2026-08-31T0225Z"
scope: "scripts/sync-templates.js, templates/*, skills/godkit-handoff/SKILL.md, package.json, .gitattributes, tests/{cli,package}.test.js"
status: "done"
skills: ""
---

## Task

T-014 — audit `templates/`: confirm every file is consumed, nothing is orphaned, and tighten what
is not.

## Did

- new `scripts/sync-templates.js` — generates the task and log frontmatter blocks in
  `skills/godkit-handoff/SKILL.md` from `templates/task.md` and `templates/log.md`. Mirrors
  `sync-rules.js`: no flag regenerates, `--check` fails on drift.
- `package.json:check` — `sync-templates --check` added to the chain, so `pretest` runs it.
- `templates/task.md`, `templates/log.md` — dead placeholders replaced with blanks plus a comment
  naming the expected shape.
- `skills/godkit-handoff/SKILL.md` — named markers around both frontmatter blocks; each generated
  blank form is followed by a hand-owned filled example.
- `templates/gitattributes` + this repo's `.gitattributes` — added `.agent/SKILLS.md -merge`.
- `tests/cli.test.js` — init test extended; new test asserting no `{{` survives anywhere under
  `.agent/`.
- `tests/package.test.js` — new test asserting the skill's blocks match the templates verbatim.

## Verified

- `npm test` -> 190 tests, 188 passed, 0 failed, 2 skipped.
- Falsification: renaming `owner:` to `holder:` in `templates/task.md` gives
  `node scripts/sync-templates.js --check` exit 1, `npm run check` exit 1, and `npm test` exit 1
  through `pretest`. Restoring returns all three to 0. (Measured with `$?` directly, not through a
  pipe — the first attempt read `tail`'s exit code and looked like a pass.)
- End to end, scratch repo at `.../scratchpad/ta`: `godkit init` landed `BOARD.md`, `THREAD.md`,
  `MAP.md`, `.agentignore`, `tasks/`, `log/`; `grep -rn "{{" .agent/` -> nothing;
  `.gitattributes` -> `.agent/SKILLS.md  -merge`; `head -1` of BOARD and THREAD both `— ta`.
- Trace behind the finding: `grep -rn "task\.md|log\.md"` over the repo hits only
  `tests/contract.test.js:163,217`, `tests/evolve.test.js:320` and `.agent/` map data;
  `grep -rn "templates/" skills/ AGENTS.md README.md` -> nothing.

## Bugs

- found B-013 — **`godkit init` must never be run inside the godkit repo itself.** I ran it to
  refresh the managed `.gitattributes` block and it appended 81 lines of AGENTS.md's own body back
  into `AGENTS.md`, and the same into `CLAUDE.md` and `.cursor/rules/godkit.mdc`. Root cause is not
  a defect in `applyBlock`: appending a marked block to a host file that has none is exactly what
  `init` is for, and `tests/cli.test.js` pins that behaviour. It is self-reference — here
  `AGENTS.md` is the *generator source*, not a managed copy. Caught on the same turn from the
  "appended" lines in init's own output and reverted with `git checkout --`; this repo's
  `.gitattributes` is hand-maintained (it has no godkit markers and its own longer comment), so it
  was edited directly instead. Open as a documentation gap, not a code fix.
- fixed, pre-commit — `templates/task.md` and the skill's inline copy disagreed on field order
  (`exit` before `phase` vs after `blocked`). Root cause was two hand-maintained copies, cut at the
  source by generating the skill's block.

## Decisions

- Only the frontmatter is generated, not the bodies. Generating the log body too would have
  replaced its worked example (real `## Did` / `## Verified` bullets) with a blank form, which
  teaches less than what it replaced.
- Not `lib/managed.js` for the injection: `locate()` refuses more than one marker pair per file
  because it guards files the user co-owns, and this file needs two. Fifteen lines of named-marker
  replace in the script beat loosening a safety guard used by `init`.
- Left alone after checking: `.agentignore` vs `ALWAYS_SKIP` is layering (hard floor before the
  user-editable list, which can express globs the floor cannot); the two frontmatter parsers have
  genuinely different contracts; `.agent/THREAD.md.bak` is untracked and matched by `.gitignore:3`.

## Left / next

- B-013 is unaddressed in prose. `AGENTS.md` or `README.md` "Development" could say plainly that
  `godkit init` is for other projects, never this one. One sentence, deliberately not bundled here.
- `.agent/MAP.md` still stale — does not know `lib/contract.js`, `scripts/sync-templates.js` or
  `skills/godkit-triage/`. Needs its own `scan` + `save` session.
- did NOT touch `lib/` at all, so nothing here overlaps T-012's `godkit/windows-ci` or T-013's
  `godkit/triage`.
