---
agent: claude
session: t007-package-docs
started: 2026-08-22T15:50:00Z
ended: 2026-08-22T16:12:00Z
scope: package.json, .github/workflows, scripts/check-versions.js, templates/gitattributes, README.md, docs/agent-portability.md, skills/godkit-evolve/SKILL.md, tests/package.test.js
status: done
skills: godkit-lazy
---

## Task

T-007: make the shipped package, its release gates and its public claims true.

## Did

- Found and fixed a real allowlist break: `godkit init` read `.gitattributes` from the repo root,
  which npm does not ship. Moved those lines to `templates/gitattributes`.
- Added a packed-install smoke test that copies the actual `npm pack` file list into a temp
  directory and runs `godkit init` from it.
- Tightened the tag gate so an empty or non-release tag fails instead of skipping the check, and
  made `publish.yml` tag-only.
- Wired `scripts/sync-hooks.js --check` into `npm run check` and both workflows.
- Rewrote the install, hooks, CLI and project-skill sections of README and the portability doc to
  match what T-002 through T-006 actually built.

## Verified

- `npm test` -> 160 passed, 0 failed, 1 skipped.
- `npm run check` -> rule copies, hook manifest and versions all in sync.
- `npm pack --dry-run` -> 77 files; the packed set runs `godkit init` successfully.
- Tag gate exercised at v1.0.0 (pass), v9.9.9 (fail), empty (fail).
- `git diff --check` -> clean.

## Bugs

- B-009 addressed: allowlist, publish gate and doc drift. Marked closed on the board by T-008.

## Decisions

- Did not rewrite `gemini-extension.json` or the Codex manifest to a guessed schema. Their extra
  pointers are unverifiable from this machine; the doc now says which fields are verified and
  which are best-effort rather than inventing certainty.
- Did not build a command installer. `commands/*.toml` reaches hosts through a plugin or extension
  install; dropping fourteen files into a user's own commands directory is not ours to do.

## Left / next

- T-008: final join, map rebuild, close B-009 and B-010, release every claim.
