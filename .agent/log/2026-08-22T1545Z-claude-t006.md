---
agent: claude
session: t006-cli-integration
started: 2026-08-22T15:25:00Z
ended: 2026-08-22T15:50:00Z
scope: bin/godkit.js, lib/freshness.js, lib/managed.js, tests/cli.test.js
status: done
skills: godkit-lazy
---

## Task

T-006: managed init blocks, honest freshness, a save that forgets deleted files, and hook
lifecycle commands.

## Did

- Added `lib/managed.js` and moved `godkit init` onto it, so a project that already had a
  CLAUDE.md now gets the rules instead of being skipped, and keeps everything it had.
- Extended init to manage `.gitattributes`, which target projects never received.
- Dropped map nodes whose file no longer exists during a partial save.
- Routed MAP.md and meta.json through `atomicWriteFile`.
- Made `staleness()` fail closed on every git answer it cannot get, and parse `-z` status through
  `statusPaths` so a rename no longer becomes a bogus path.
- Added `godkit hooks [status|install|uninstall] [--dry-run]`, put hook registration in `doctor`,
  and deleted `link()` in favour of `installOne`/`removeOne`.

## Verified

- `node --test tests/cli.test.js` -> 17 passed, 0 failed.
- `npm test` -> 158 passed, 0 failed, 1 skipped.
- `node bin/godkit.js doctor` and `hooks status` run clean against this machine, read-only.
- `git diff --check` -> clean.

## Bugs

- B-003 and B-004 closed. B-008 closed by the managed-block init.

## Decisions

- Freshness reports `unknown` rather than `fresh` whenever git cannot answer. A hook that says
  "current" about a map nobody verified is worse than one that says it does not know.
- Deleted-node detection is by existence on disk, not by a declared refresh scope. Existence is
  the actual invariant, and it needs no new argument threaded through the map skill.

## Left / next

- T-007 owns docs and `npm run check`; both handoff items are recorded in the T-006 task file.
