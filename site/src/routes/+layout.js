// Prerendered AND hydrated: the content is in the served HTML (so it reads with JS off and
// indexes properly), and the client pass is what layers the GSAP motion on top of it.
export const prerender = true
export const ssr = true
export const csr = true
